<% provide(:title, 'All users') %>
<h1>All users</h1>

<div class="pagination col-md-8">
  <%= will_paginate @users %>
</div>

<ul class="users">
  <%= render @users %>
</ul>

<div class="pagination col-md-8">
  <%= will_paginate @users %>
</div>
